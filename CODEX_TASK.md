# Current Codex Cloud Task: M2 Consent-Bound Identity Profiles

Implement M2 from `tasks/M2_IDENTITY_PROFILES.md` on the current Draft pull request.

Do not add publishing integrations or real voice/avatar inference. Do not commit media, credentials, signed URLs, model weights, or embeddings.

The goal is to make self-owned voice/avatar profile consent, private media intake, revocation, deletion, and downstream invalidation production-grade before any cloning/lip-sync provider is enabled.
